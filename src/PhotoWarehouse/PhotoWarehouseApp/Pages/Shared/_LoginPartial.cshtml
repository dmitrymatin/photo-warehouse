@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <a class="navItem clickable" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity.Name</a>
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
        <button type="submit" class="navItem clickable">Выход</button>
    </form>

}
else
{
    <a class="navItem clickable" asp-area="Identity" asp-page="/Account/Register">Регистрация</a>
    <a class="navItem clickable" asp-area="Identity" asp-page="/Account/Login">Вход</a>
}
